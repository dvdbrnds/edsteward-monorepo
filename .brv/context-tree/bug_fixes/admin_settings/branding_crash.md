Fixed critical branding crash in EdSteward admin settings. Issue: ReferenceError "type is not defined" in FileUploadField component in client/src/components/admin/branding-settings.tsx. Root cause: Parameter destructuring error where 'type' prop was renamed to '_type' but code still referenced 'type'. Fix: Changed line 141 from `type: _type` to `type` in destructuring parameters. This resolved the JavaScript error that crashed the system when clicking on the branding tab in admin settings. Tested successfully - branding functionality now works without crashes. Frontend rebuilt and server restarted to apply fix.