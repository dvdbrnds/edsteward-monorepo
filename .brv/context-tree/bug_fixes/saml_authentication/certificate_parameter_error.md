CRITICAL SAML DEBUGGING DISCOVERY: The certificate is loading perfectly (1434 characters, proper PEM format with headers), all validation passes, but the error "TypeError: cert is required" occurs inside node-saml library at saml.ts:71. This suggests a parameter mapping issue between passport-saml (which uses `idpCert`) and the underlying node-saml library (which expects `cert`). The certificate data is correct but not being passed properly to the underlying SAML implementation.