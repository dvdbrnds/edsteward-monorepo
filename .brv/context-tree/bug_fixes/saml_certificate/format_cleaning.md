CRITICAL SAML CERTIFICATE DEBUGGING: EdSteward SAML implementation is failing at the SamlStrategy creation step despite certificate loading successfully (1434 characters). The error "cert is required" suggests the passport-saml library expects a specific certificate format. The certificate is being read from file correctly but may need format cleaning (removing headers/footers or fixing line endings) before passing to the SAML strategy constructor.