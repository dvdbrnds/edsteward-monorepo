CRITICAL SSO DEBUGGING DISCOVERY: EdSteward SAML configuration failing due to certificate parsing issues. Server logs show "🔐 Authentication: Username/Password" instead of SAML enabled despite AUTH_SAML_ENABLED=true in .env. The certificate format in .env with \n escape sequences is not being parsed correctly by the Node.js environment. Need to investigate alternative certificate storage methods or fix the parsing logic in server/config/institution.ts.