SAML CERTIFICATE FIX: Replaced the problematic certificate with the original OKTA certificate file from /Users/dvdbrnds/Desktop/okta.cert. The original certificate has proper formatting without line breaks in the base64 content. Updated SAML strategy to use cleaned certificate in array format `cert: [cleanCert]` and re-enabled `wantAssertionsSigned: true` while keeping `validateInResponseTo: false` and `disableRequestedAuthnContext: true`. SAML endpoint is responding correctly with 302 redirect to OKTA.