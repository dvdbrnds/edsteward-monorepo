SAML Production Deployment Issue: After updating task definition with correct production OKTA URLs (moravian_edsteward_1), the application is still redirecting to the beta URL (moravian_edstewardbeta_1). This suggests the Docker image was built with the old configuration. Need to rebuild Docker image with updated task definition environment variables to ensure production OKTA configuration is properly applied.