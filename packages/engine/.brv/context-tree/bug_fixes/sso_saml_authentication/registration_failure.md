CRITICAL SSO DEBUGGING INSIGHT: The authentication function `configureAuth` is not being called despite creating the missing `server/auth/index.ts` file. Server logs show "🔐 Authentication: Username/Password" but this message doesn't exist in the current codebase, suggesting there's a cached version or different authentication system being used. The SAML routes are not being registered because the authentication setup function is never executed. This indicates either a Docker build cache issue or the wrong authentication module is being imported.