CRITICAL SSO ANALYSIS: EdSteward already has comprehensive SAML authentication system implemented with @node-saml/passport-saml v4.0.4. Found existing SAML configurations in server/auth/saml.ts with multi-IDP support (Okta, Shibboleth, InCommon). Key files: server/auth/saml.ts (multi-SAML), server/auth/single-tenant-auth.ts (single tenant SAML), server/config/saml.ts (IDP configs), OKTA_SAML_SETUP_GUIDE.md (complete setup guide). Environment variables needed: OKTA_SSO_URL, OKTA_CERT, OKTA_SLO_URL, OKTA_ENTITY_ID, BASE_URL, SAML_SP_ENTITY_ID. Previous failure memory shows certificate formatting issues (literal \n vs newlines) and ECS deployment problems. Must test locally first before AWS deployment.