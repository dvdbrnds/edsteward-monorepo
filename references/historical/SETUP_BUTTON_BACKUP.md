
# Setup Button Backup

This is a backup of the "Skip Setup" button code that was temporarily removed from the setup wizard page.

```tsx
<Button
  type="button"
  variant="ghost"
  onClick={() => setIsComplete(true)}
>
  Skip Setup
</Button>
```

Location: client/src/pages/setup-wizard-page.tsx in the office assignment form
